---
title: By date
description: A list of CSS frameworks sorted by date
layout: base
tags: [list2]
---

{% assign list = frameworks | map: 'type' | uniq %}

{% for item in list %}

<h2><strong>{{ item }}</strong></h2>

{% assign framework = frameworks | where: "type", item | sort: 'published' | reverse | sort: 'type' %}

    {% include 'table3' %}

{% endfor %}