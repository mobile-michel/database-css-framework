---
title: Heavy class
description: CSS frameworks with lot's of classes and features
layout: base
tags: [category]
---


{% assign items = frameworks | sort: 'name' %}

{% for item in items %}
  {% if item.category == 'heavyclass' %}
    {% include 'filters' %}
  {% endif %}
{% endfor %}

